# Criando Dashboard usando Flask

### Instalando uma virtualenv
`pip install virtualenv`


### Iniciando a virtualenv
`virtualenv -p python3 venv`


### Executando o ambiente virtual
`. venv/bin/activate`


### Instalando módulos
```sh
pip install flask
pip install pucher
```